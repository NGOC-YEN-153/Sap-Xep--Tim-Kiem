#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
    ll n;cin>>n;
    ll a[n];
    set<int> v;
    for(ll &x:a){
        cin>>x;
        while(x>0){
            ll ok=x%10;
            v.insert(ok);
            x/=10;
        }
        
    }
    for(auto it:v) cout<<it<<" ";
}
